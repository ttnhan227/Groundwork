# System Architecture & Design Constraints

This document details the architectural blueprints, multi-tenant security guarantees, code-level constraints, and AI orchestration pipelines governing Groundwork.

---

## 1. System Overview & Core Principles

Groundwork is an enterprise-grade **Verification-Gated Document Studio**. Unlike standard LLM chatbots or wrapper apps, Groundwork enforces strict deterministic verification, audit trails, and multi-tenant security boundaries before any deliverable can be marked verified or exported.

```
┌─────────────────┐       ┌────────────────────────┐       ┌──────────────────────┐
│  Source Inputs  │ ────► │  Workspace Context &   │ ────► │  Verification Gate   │
│  (PDFs, Docs,   │       │   Agent Orchestrator   │       │ (Readiness & Export) │
│   Specs, RFPs)  │       │  (Draft / Refine / QA) │       │ (100% Audit Passed)  │
└─────────────────┘       └────────────────────────┘       └──────────────────────┘
```

---

## 2. The 5 Core Architectural Constraints & Guardrails

Groundwork operates under five non-negotiable architectural laws:

### 🛡️ 1. Multi-Tenant Data Access Guard (Lá Chắn Truy Cập Dữ Liệu)
* **Rule**: All database operations against workspace entities (`NativeDocument`, `Document`, `DeliverableRequirement`, `DeliverableReviewFinding`, `WorkspaceMemory`) must strictly scope through `workspace_access(workspace_id, user, session)` or verify tenant ownership.
* **Automated Guard Test**: `server/tests/test_data_access_guard.py` performs AST parsing across all FastAPI route handlers on every CI build to verify tenant scoping and block unscoped database queries.

### 🎯 2. Code-Level Citation & Hallucination Contracts (Ràng Buộc Ép Ở Tầng Mã)
* **Rule**: Anti-hallucination guarantees are enforced in Python code, not merely requested in prompt instructions.
* **Mechanism (`_validate_physical_citations`)**:
  1. Every citation generated by the AI (`[Source: document.pdf, p. 12]`) is verified against physical `Document` and `DocumentPage` records in the database.
  2. If an AI model hallucinates a document name or references a page exceeding the document's physical page count, the citation is stripped and automatically recorded as a `source_conflict` / `unsupported_claim` audit finding.
  3. Requirements cannot be marked `COVERED` unless grounded by verified physical pages.

### ⚡ 3. AI Idempotency & Deduplication Engine
* **Rule**: The system prevents duplicate LLM calls, concurrency races, and wasted tokens from repeated button clicks.
* **Mechanism**:
  - Deterministic SHA-256 idempotency keys: `sha256(user_id + workspace_id + action_type + prompt + sorted_source_ids)`.
  - Rapid duplicate executions within the debounce window are deduplicated and logged.

### 📊 4. Token & Cost Telemetry Governance (`AIUsageRecord`)
* **Rule**: Every model execution records full operational telemetry:
  - `prompt_tokens`, `completion_tokens`, `total_tokens`
  - Execution latency (`latency_ms`)
  - Estimated cost (`cost_usd`)
  - Feature operation name (`workspace_agent.draft`, `workspace_agent.grounded_qa`, `deliverable_review`)
* Telemetry feeds the **Usage & Billing** tab in Account Settings.

### 🌐 5. Deep Workspace Context & Multi-Language Engine
* **Rule**: The AI agent operates with complete visibility into the workspace state rather than isolated chat threads.
* **Snapshot (`_build_workspace_context_snapshot`)**:
  - Workspace identity & notes (`WorkspaceMemory`)
  - Active deliverable draft content & revision
  - Requirements Traceability Matrix (`COVERED`, `PENDING`, `WAIVED`)
  - Open audit findings & unverified claims
  - Active source document catalog
  - Full native localization across 9 languages (`en`, `vi`, `es`, `ja`, `de`, `fr`, `zh`, `ko`, `pt`).

---

## 3. High-Level Component Topology

```
                      ┌──────────────────────────────────────────────┐
                      │              Browser Client (SPA)            │
                      │  React 19 + TypeScript + Vite + SSE Client   │
                      └──────────────────────┬───────────────────────┘
                                             │ HTTP / SSE / WS
                                             ▼
                      ┌──────────────────────────────────────────────┐
                      │             Nginx Reverse Proxy              │
                      └──────────────────────┬───────────────────────┘
                                             │
                      ┌──────────────────────▼───────────────────────┐
                      │         FastAPI Backend Application          │
                      │  - Controllers & Data Access Guards          │
                      │  - Workspace Agent & Orchestration Engine    │
                      │  - Whole-Document Verification Engine        │
                      └──────────────┬────────────────┬──────────────┘
                                     │                │
             ┌───────────────────────▼──┐          ┌──▼────────────────────────┐
             │   PostgreSQL 16 Engine   │          │   Celery Background Queue │
             │  - pgvector Embeddings   │          │   + Redis Message Broker  │
             │  - Relational Schemas    │          │  - PDF OCR & Page Parsing │
             │  - AIUsageRecord Logs    │          │  - Async Embeddings Gen   │
             └──────────────────────────┘          └──────────────┬────────────┘
                                                                  │
                                                   ┌──────────────▼────────────┐
                                                   │    MinIO Object Storage   │
                                                   │   (Encrypted S3 Storage)  │
                                                   └───────────────────────────┘
```

---

## 4. Verification Lifecycle State Machine

```
[ Upload Sources ] ──► [ Extract Requirements ] ──► [ Agent Draft Deliverable ]
                                                           │
                                                           ▼
[ Blocked from Export ] ◄── (Readiness < 100%) ◄── [ Run Whole-Document Audit ]
         │                                                 │
         │ (Apply Verified Revision)                       │ (0 Unsupported Claims)
         ▼                                                 ▼
[ Update Revision & Citations ] ──────────────────► [ Readiness = 100% (Verified) ]
                                                           │
                                                           ▼
                                                    [ Export Gate UNLOCKED ]
                                                    - Download PDF / DOCX / MD
                                                    - Cryptographic Audit Appendix
```
